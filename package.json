{
  "name": "regitra",
  "version": "0.0.0",
  "scripts": {
    "build:json": "node ./builder/json.js",
    "dev": "npm run build:json && vite",
    "build": "npm run build:json && tsc && vite build",
    "serve": "npm run build:json && vite preview",
    "test:unit": "vitest run",
    "fmt": "prettier --write ."
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^4.1.1",
    "autoprefixer": "^10.4.15",
    "postcss": "^8.4.29",
    "prettier": "^3.0.3",
    "terser": "^5.19.4",
    "typescript": "^5.2.2",
    "vite": "^4.4.9",
    "vitest": "^0.34.4"
  },
  "dependencies": {
    "draggable_dialog": "^1.0.3"
  }
}
